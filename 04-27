(define w (id (id 10)))

count
> 1 (1)

w
> 10 (2)

count
> 2 (3)

(1) (define w..)の時点で(id (id 10))が評価される。まず、(id (id 10))の
(set! count (+ count 1)
が評価され、countは1となる。次にxとして(id 10)が返されるが、この評価は遅延されている。
(2) 次にwを呼出したとき、(1)で評価が遅延されていた(id 10)が実行される。そこで
(set! count (+ count 1)
が評価されるのでcountは2となり、さらにxとして10が返される。
したがって、入力wによって出力10を得る。
(3) (2)の処理により、countの値は2となっている。
